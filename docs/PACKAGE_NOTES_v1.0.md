# Package Notes v1.0

本压缩包包含可运行 Demo 代码 v1.0 和对应文档。

## 版本重点

- 新增 Omni Session Bridge。
- 新增 `omni.input_packet.v1` 输入包。
- 新增 `omni.output_turn.v1` Mock 输出回合。
- 保留 v0.9 的实时音频、网络、关键帧策略、身份档案、插件 manifest 和权限执行链。

## 运行

```bash
npm install
npm run dev
```

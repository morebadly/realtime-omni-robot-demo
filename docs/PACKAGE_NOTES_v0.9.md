# Package Notes v0.9

这个压缩包是可运行项目更新包，不包含 `node_modules` 和 `dist`。

## 运行

```cmd
npm install
npm run dev
```

## 验证重点

- v0.9 页面标题。
- Realtime Audio Stream 面板。
- Network / Connection Manager 面板。
- FramePolicyEngine 是否随模式、网络质量、说话状态、视觉问答变化。
- CameraPreview 是否读取 Runtime 的 FramePolicy。
